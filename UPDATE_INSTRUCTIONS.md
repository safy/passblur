# 🔄 Инструкция по обновлению PassBlur

## Проблема решена! ✅

Если расширение не обнаруживало API ключи на страницах вроде Google Cloud Console, это исправлено в версии 1.1.0.

---

## Как обновить расширение

### Способ 1: Быстрое обновление (Рекомендуется)

1. **Откройте Chrome Extensions:**
   ```
   chrome://extensions/
   ```

2. **Найдите PassBlur и нажмите кнопку "Обновить"** (круговая стрелка)
   - Если кнопки нет, включите "Режим разработчика" вверху справа

3. **Готово!** Расширение обновлено

4. **Перезагрузите страницы** где нужна защита API ключей
   - Нажмите F5 или Ctrl+R на нужных страницах

### Способ 2: Переустановка (Если способ 1 не помог)

1. **Откройте Chrome Extensions:**
   ```
   chrome://extensions/
   ```

2. **Удалите старую версию PassBlur**
   - Нажмите "Удалить" на карточке PassBlur

3. **Установите заново:**
   - Нажмите "Загрузить распакованное расширение"
   - Выберите папку `D:\passblur`

4. **Перезагрузите страницы** с API ключами

---

## Проверка работы

### Тест 1: Проверка версии

1. Откройте popup расширения (клик на иконку 🔒)
2. Внизу должно быть написано: `v1.0.0` (версия в manifest)

### Тест 2: Проверка на тестовой странице

1. Откройте `test.html` в Chrome
2. Все ключи должны быть размыты
3. Счетчик в popup должен показывать 25+ ключей

### Тест 3: Проверка на Google Cloud Console

1. Откройте страницу с API ключом в Google Cloud Console
2. Если ключ виден в модальном окне или input поле:
   - Должен появиться фиолетовый overlay с blur эффектом
   - При наведении должен появиться tooltip
   - При клике ключ копируется

3. Откройте popup расширения:
   - Счетчик "Защищенных ключей" должен быть > 0
   - Статус должен быть "Активен" (зеленый)

### Тест 4: Если не работает сразу

1. **Нажмите "Пересканировать страницу"** в popup
2. Подождите 3-5 секунд
3. Проверьте счетчик снова

---

## Что изменилось в версии 1.1.0

### ✅ Исправлено

- **Главное**: Теперь обнаруживаются ключи в input полях и textarea
- **Главное**: Работает на Google Cloud Console и подобных сайтах
- Улучшено обнаружение динамически загружаемого контента
- Оптимизирована производительность MutationObserver

### ✨ Новое

- Визуальный overlay с blur для input полей
- Автоматическое сканирование с задержкой (1 и 3 секунды)
- Более гибкие regex паттерны для Google API
- Улучшенная обработка модальных окон

### 📊 Статистика

- Обнаруживает ключи в текстовых узлах ✅
- Обнаруживает ключи в input полях ✅ (новое!)
- Обнаруживает ключи в textarea ✅ (новое!)
- Обнаруживает ключи в pre/code ✅ (улучшено!)

---

## Частые вопросы

### Q: Нужно ли что-то делать с иконками?

**A:** Нет, иконки остаются прежними.

### Q: Сохранятся ли мои настройки?

**A:** Да, все настройки в `chrome.storage.sync` сохраняются.

### Q: Что если ключи все равно не размываются?

**A:** 
1. Откройте popup и нажмите "Пересканировать страницу"
2. Попробуйте перезагрузить страницу (F5)
3. Проверьте что защита включена (зеленый переключатель)
4. Откройте DevTools (F12) и посмотрите в Console на ошибки

### Q: Работает ли на всех сайтах?

**A:** Да, но:
- Не работает на служебных страницах Chrome (chrome://, about://)
- Может не работать в Shadow DOM (редкие случаи)
- Не работает в PDF файлах

### Q: Как проверить что обновление установилось?

**A:**
1. Откройте `chrome://extensions/`
2. Включите "Режим разработчика"
3. Найдите PassBlur
4. Нажмите "Подробнее"
5. Посмотрите дату последнего обновления

---

## Для разработчиков

### Измененные файлы

```
content-script.js   - Добавлено ~100 строк кода
content-style.css   - Добавлено ~65 строк стилей
CHANGELOG.md        - Новый файл
UPDATE_INSTRUCTIONS.md - Новый файл (этот)
```

### Тестирование изменений

```javascript
// В консоли браузера на странице с API ключом:

// 1. Проверить что расширение загружено
console.log('PassBlur loaded:', !!document.querySelector('.passblur-wrapper, .passblur-input-overlay'));

// 2. Проверить количество защищенных элементов
console.log('Protected elements:', {
  text: document.querySelectorAll('.passblur-wrapper').length,
  inputs: document.querySelectorAll('.passblur-input-overlay').length
});

// 3. Проверить паттерн Google API
const testKey = 'AIzaSyBflUdCarTP42tBegnkFt8qz6RxCsSWjh0';
const pattern = /\b(A[Il]za[0-9A-Za-z_-]{30,})\b/g;
console.log('Pattern test:', pattern.test(testKey));
```

---

## Поддержка

Если проблема не решена после обновления:

1. Откройте DevTools (F12) на странице с ключом
2. Посмотрите в Console на ошибки
3. Сделайте скриншот
4. Создайте Issue с описанием проблемы

---

**Обновление установлено! Наслаждайтесь улучшенной защитой! 🔒✨**

